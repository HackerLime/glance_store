import { createSlice } from '@reduxjs/toolkit';

const initialState: number[] | [] = []


const basketSlice = createSlice({
  name: 'basket',
  initialState,
  reducers: {
    addData() {

    }
  }
})